const search = (age, earning)=>{
   console.log(trueAge);
   //hosting
   console.log(age);
   console.log(earning);

   const trueAge =age +7;
   const canMery = (expense) => {
      const remaining = earning - expense;
       if(remaining >1000){
           return true;
       }
       else{
           return false;
       }
    
    }  
}

search(21, 20000)